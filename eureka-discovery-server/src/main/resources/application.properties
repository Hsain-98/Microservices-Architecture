server.port=8761



#-- Commented as already specified in application.yml
# Server doesnot required to register it self hence set to false
# Server Does not required to fetch the registry it self
spring.application.name=eureka
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
#eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/eureka/
eureka.server.wait-time-in-ms-when-sync-empty=0